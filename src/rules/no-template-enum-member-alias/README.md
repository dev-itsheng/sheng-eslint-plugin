# @sheng/no-template-enum-member-alias

提示 Vue template 可以直接使用 script setup 里的 enum member，不需要额外声明一比一中转常量。

完整规则文档见 `../../../docs/rules/no-template-enum-member-alias.md`。

内置 config：`enum-public-api`。
